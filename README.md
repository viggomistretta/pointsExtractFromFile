# pointsExtractFromFile
program that can give all SRE points with associated coordinates from a variable type of file (.ls, etc...) from robot type ABB, FANUC, KUKA, etc. in order to export them in a .csv to compare the program values ​​with the real values.
